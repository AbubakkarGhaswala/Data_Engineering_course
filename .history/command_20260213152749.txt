------- Hello Everyone --------



GIt HUB:—


Now It’s All command 

Pwd - Present working directory (this will show our current directory)

Mkdir folder name - it will create a folder 

Cd foldername -  cd stand for change directory and folder name so it will move to that folder

Nano filename.extenstion - this command is use to create a file inside folder 

Cat filename - this command will show the content of the file 

LS - this command will show the list of files inside the folder 

Git status - this command will show the current status of our file like is it on push state or not create a directory 

Git init - this command is use to intilalize the connection with GitHub 

Git log - this command will show how many change has been done like how many commits has done and all

Git add . - this command will add all files in current directory to the repository 

Git add filename - this command will add only this file 

Now after git add command we have to commit it 

Git commit -m “Message Any “ -  this will commit the add it is one type of cntrl + s 

Git config — global user.name “Name”
Git config — global user.mail “email” this will help you to change the author username and email 

All set! Now it’s time to connect our local folder to GitHub remote repository so first create a repository in github and then  we get the url after create a repo then we have to comeback at our local and write 

Git remote add origin url - this will connect our local repo to remote githuub repo

Now it’s time to push our files from local to our remote repository 

Git branch -M main - this will rename the branch name to main 

Git push -u origin  branch name - this will push our files from local repo to GitHub remote repo

Git branch branch name - this will create a branch inside our repo 

Git checkout -t :- move from one branch to another branch 

Git checkout -b branch name -  this command will do if branch is not there then it create a branch and move there 

If we will take the commit id and then write 
Git showcommit id :- sp ot will show what changes has done in which file 


Git merge branchname - so this will merge your branch with main 

If we don’t have a local folder and it’s available on our GitHub repo so how to pull that repo 

Git clone url of that repo 
