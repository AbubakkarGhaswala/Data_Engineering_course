For install the kafka first let setup everything for kafka we need java runtime environment so install that 
then i will give one kafka folder open that folder in terminal and run 

bin/kafka-storage.sh random-uuid

this command if i will get permission denied or something so run 

chmod +x bin/Kafka-storage.sh this and 
chmod +x bin/*.sh
this command and then run 

bin/kafka-storage.sh random-uuid
 this command again so we will get one cluster id 

ADi8TuHbTo-4KbauT3_wFg





curl -o kafka_2.13-4.0.0.tgz https://dlcdn.apache.org/kafka/4.0.0/kafka_2.13-4.0.0.tgz
extraction>>tar -xzf kafka_2.13-3.8.0.tgz

bin\windows\kafka-storage.bat random-uuid    (for mac bin\kafka-storage.sh random-uuid)

set KAFKA_CLUSTER_ID=9e2a22c9-4c5d-4f1b-a6a3-2bb5f3b1d123
BmUMStDcT9ihygAtG-A01A



(repeat process whenever we want to use kafka)
so when all this done we just need to start the kafka server 

bin/kafka-server-start.sh config/kraft/server.properties


then we have to open new tab with same folder and run 

bin/kafka-topics.sh --create --topic quickstart-events --bootstrap-server localhost:9092 
this command just change the topic if we want to start new topic 

then bin/kafka-topics.sh --list --bootstrap-server localhost:9092 

verify the topic is created or not then 

bin/kafka-console-producer.sh --topic quickstart-events --bootstrap-server localhost:9092 

we have to run this command so we will be a producer then same folder with new terminal tab 

now as a consumer 

bin/kafka-console-consumer.sh --topic quickstart-events --from-beginning --bootstrap-server localhost:9092

this only. 


now what i will gonna do is i will make it easy for me i will not gonna remember this longs command so what i will do is 
i will make alias of that command just like in python we write import panads as pd so pd is alias version of pandas 


so first open that kafka folder in terminal and write 

nano ~/.zshrc


so we will get one file popuo inside the file there will be a path of something so write 

alias kafkastart='bin/kafka-server-start.sh config/kraft/server.properties'
alias kafkatopic='bin/kafka-topics.sh --bootstrap-server localhost:9092'
alias kafkaproducer='bin/kafka-console-producer.sh --bootstrap-server localhost:9092'
alias kafkaconsumer='bin/kafka-console-consumer.sh --bootstrap-server localhost:9092' 

this alias after that path at the bottom of file then press cntrl + o press enter then cntrl + x 


then activate the .zshrc so write 


source ~/.zshrc



then if we want to start the server just write kafka